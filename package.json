{
  "name": "f4fun",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @f4fun/shared-types build && pnpm --filter @f4fun/monopoly-engine build && concurrently -n server,web -c blue,magenta \"pnpm --filter @f4fun/server dev\" \"pnpm --filter @f4fun/web dev\"",
    "build": "pnpm -r build",
    "start": "pnpm --filter @f4fun/server start",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.7.3"
  }
}
